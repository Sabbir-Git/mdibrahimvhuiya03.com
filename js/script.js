/* === typing animation == */
var typed = new Typed(".typing",{
    strings:["Student","Web-developer"],
    typeSpeed:100,
    BackSpeed:60,
    loop:true
});
/* === Aside == */
const nav = document.querySelector(".nav"),
        navList = nav.querySelector("li"),
        totalNavList = nvList.length;
        for(let i=0; i<totalNavList; i++)
        {
            console.log(navList[i])
        }
